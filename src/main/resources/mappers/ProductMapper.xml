<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.phone_store.mapper.ProductMapper">

    <resultMap id="ProductResultMap" type="org.example.phone_store.entity.Product">
        <id     column="ProductId"    property="productId"/>
        <result column="ProductName"  property="productName"/>
        <result column="Description"  property="description"/>
        <result column="Price"        property="price"/>
        <result column="Quantity"     property="quantity"/>
        <result column="ImageUrl"     property="imageUrl"/>
        <result column="CategoryId"   property="categoryId"/>
        <result column="CreatedAt"    property="createdAt"/>
    </resultMap>

    <select id="getAllProducts" resultMap="ProductResultMap">
        SELECT ProductId, ProductName, Description, Price, Quantity,
               ImageUrl, CategoryId, CreatedAt
        FROM Products
        ORDER BY CreatedAt DESC
    </select>

    <select id="searchProducts" resultMap="ProductResultMap">
        SELECT ProductId, ProductName, Description, Price, Quantity,
               ImageUrl, CategoryId, CreatedAt
        FROM Products
        WHERE ProductName LIKE CONCAT('%', #{keyword}, '%')
           OR Description LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY CreatedAt DESC
    </select>

    <select id="getProductById" resultMap="ProductResultMap">
        SELECT ProductId, ProductName, Description, Price, Quantity,
               ImageUrl, CategoryId, CreatedAt
        FROM Products
        WHERE ProductId = #{productId}
    </select>

    <insert id="insertProduct" parameterType="org.example.phone_store.entity.Product"
            useGeneratedKeys="true" keyProperty="productId">
        INSERT INTO Products (ProductName, Description, Price, Quantity, ImageUrl, CategoryId)
        VALUES (#{productName}, #{description}, #{price}, #{quantity}, #{imageUrl}, #{categoryId})
    </insert>

    <update id="updateProduct" parameterType="org.example.phone_store.entity.Product">
        UPDATE Products
        SET ProductName = #{productName},
            Description   = #{description},
            Price         = #{price},
            Quantity      = #{quantity},
            ImageUrl      = #{imageUrl},
            CategoryId    = #{categoryId}
        WHERE ProductId = #{productId}
    </update>

    <delete id="deleteProduct">
        DELETE FROM Products
        WHERE ProductId = #{productId}
    </delete>

</mapper>